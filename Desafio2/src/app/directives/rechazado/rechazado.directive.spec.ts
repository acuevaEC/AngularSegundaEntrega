import { RechazadonDirective } from './rechazado.directive';

describe('RechazadoDirective', () => {
  it('should create an instance', () => {
    const directive = new RechazadonDirective();
    expect(directive).toBeTruthy();
  });
});
