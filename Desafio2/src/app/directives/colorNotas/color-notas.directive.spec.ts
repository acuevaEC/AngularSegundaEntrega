import { ColorNotasDirective } from './color-notas.directive';

describe('ColorNotasDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNotasDirective();
    expect(directive).toBeTruthy();
  });
});
